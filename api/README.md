# dojo API

FastAPI backend for the dojo MVP. Source lives under `api/src/dojo/` and now includes DuckDB schema bootstrap, SCD2 helpers, spreadsheet import parsing, validation, and the budget/account/category/transaction/net-worth API surface.

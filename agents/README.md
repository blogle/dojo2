# Agent Notes

- `AGENTS.md` contains the authoritative repository rules.
- Use `execplan.md` before non-trivial code changes.
- Use `dojo-nix-boundaries.md` for hermetic environment constraints.
- Prefer standard open-source coding-agent workflows unless they conflict with repo rules.
- Do not add new custom skills unless repeated Dojo-specific agent failures justify them.
